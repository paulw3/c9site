---
const stages = [
  { label: 'Design', copy: 'Target-state architecture, mapped before a single workload moves.' },
  { label: 'Build', copy: 'Governed execution: migration factory or GDC deployment, whichever the destination needs.' },
  { label: 'Run', copy: 'Documentation, knowledge transfer and support to keep it running, or we hand you the keys entirely.' },
];
---

<ul class="methodology">
  {stages.map((stage, i) => (
    <li>
      <span class="methodology__index mono-tag">{String(i + 1).padStart(2, '0')}</span>
      <h4>{stage.label}</h4>
      <p>{stage.copy}</p>
    </li>
  ))}
</ul>

<style>
  .methodology {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(1.5rem, 4vw, 3rem);
  }

  .methodology li {
    padding-top: 1.25rem;
    border-top: 1px solid var(--gunmetal-line);
  }

  .methodology__index {
    color: var(--aquamarine);
  }

  .methodology h4 {
    margin-top: 0.75rem;
  }

  .methodology p {
    margin-top: 0.75rem;
    color: var(--silver);
  }

  @media (max-width: 700px) {
    .methodology {
      grid-template-columns: 1fr;
    }
  }
</style>
